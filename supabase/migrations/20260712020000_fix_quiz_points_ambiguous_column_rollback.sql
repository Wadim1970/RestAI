-- Откат вернул бы исходную (сломанную) версию функции — рефакторинг,
-- не поведенческое изменение снаружи, поэтому "откат" здесь бессмысленен.
-- Если действительно нужно вернуться к прежнему телу функции, применить
-- CREATE OR REPLACE FUNCTION public.register_guest_and_credit_quiz(...)
-- из supabase/migrations/20260711010000_quiz_points_profile.sql заново.
